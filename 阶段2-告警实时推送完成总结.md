# 阶段 2: 告警实时推送 - 完成总结

## ✅ 已完成的修改

### 1. 创建告警 Store (`src/stores/alarm.ts`)

**功能:**
- 管理告警记录列表
- 提供告警统计数据(未读数量、今日告警数、严重程度分布)
- 自动格式化告警信息(设备名称、参数名称、时间)
- 限制最大缓存数量(100条)

**核心方法:**
- `addAlarmEvent(event: AlarmEvent)`: 接收后端推送的告警事件,转换为前端展示格式
- `markAsRead(alarmId: number)`: 标记告警为已读
- `getRecentAlarms(count: number)`: 获取最近N条告警
- `clearAll()`: 清空所有告警

**计算属性:**
- `unreadCount`: 未读告警数量
- `todayCount`: 今日告警数量
- `severityStats`: 按严重程度统计(low/medium/high/critical)

**数据结构:**
```typescript
AlarmRecord {
  id: number
  content: string  // "调配罐 温度异常: 68.5 (阈值: 63.00-67.00)"
  time: string     // "2026-04-05 14:25:30"
  deviceId: string
  deviceName: string
  severity: 'low' | 'medium' | 'high' | 'critical'
  isRead: boolean
  alarmEvent: AlarmEvent  // 原始告警事件
}
```

---

### 2. 扩展后端 DataProcessor (`websocket-server/src/services/dataProcessor.ts`)

**修改内容:**
- 更新 `handleRuleAlarm()` 方法,规范化告警推送消息格式
- 消息类型从 `RULE_ALARM` 改为 `ALARM_EVENT`
- 添加时间戳和标准化的消息结构

**推送消息格式:**
```typescript
{
  type: 'ALARM_EVENT',
  data: {
    ruleId: 1,
    ruleName: '调配罐温度持续异常',
    deviceId: '1001',
    deviceType: '调配罐',
    parameterName: 'temp',
    currentValue: 68.5,
    threshold: '63.00-67.00',
    triggerTime: 1234567890,
    severity: 'medium',
    message: '温度超过上限'
  },
  timestamp: Date.now()
}
```

---

### 3. 扩展实时数据 Store (`src/stores/realtime.ts`)

**修改内容:**
1. 导入 `useAlarmStore`
2. 将 `ALARM_EVENT` 添加到关键消息类型列表(立即处理,不批量)
3. 在 `processBatchMessages()` 中添加 `ALARM_EVENT` 消息处理逻辑

**处理流程:**
```
WebSocket 消息 → realtime.ts → 识别 ALARM_EVENT → alarmStore.addAlarmEvent()
```

---

### 4. 重构监控大屏告警功能 (`src/components/dashboard/DashboardMain_new.vue`)

**修改内容:**

#### 4.1 数据源替换
- ❌ 删除静态假数据 `alarmList`
- ✅ 从 `alarmStore.getRecentAlarms(10)` 获取实时告警列表

#### 4.2 统计数据更新
- "当日异常总数" 从 `alarmStore.todayCount` 获取(实时计算)

#### 4.3 告警监听和弹窗
使用 `watch` 监听 `alarmStore.alarmRecords.length` 变化:
```typescript
watch(() => alarmStore.alarmRecords.length, (newLen, oldLen) => {
  if (newLen > oldLen) {
    const latestAlarm = alarmStore.alarmRecords[0]
    
    // 1. ElNotification 通知
    ElNotification({
      title: '🚨 新告警提醒',
      message: latestAlarm.content,
      type: 'error',
      duration: 5000
    })
    
    // 2. 自定义告警弹窗(3秒后自动关闭)
    alarmPopupVisible.value = true
  }
})
```

---

## 🔄 完整告警流程

```
┌────────────────┐
│ RuleEngine     │ (每5秒检测一次规则)
└────────┬───────┘
         │ 检测到阈值超限
         ↓
┌────────────────┐
│ DataProcessor  │ handleRuleAlarm()
└────────┬───────┘
         │ 广播 ALARM_EVENT
         ↓
┌────────────────┐
│ WebSocket      │ (端口 8080)
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ realtime.ts    │ 识别 ALARM_EVENT 类型
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ alarm.ts       │ addAlarmEvent() 转换格式
└────────┬───────┘
         │
         ↓
┌────────────────┐
│ DashboardMain  │ watch() 监听 → ElNotification + 弹窗
└────────────────┘
```

---

## 🧪 测试步骤

### 1. 启动后端
```bash
cd websocket-server
npm run dev
```

**预期输出:**
```
[RuleEngine] 从数据库加载了 4 条规则
  - [1] 调配罐温度持续异常 (调配罐, 设备全部)
  - [2] 灌装机灌装量连续异常 (灌装机, 设备全部)
  - [3] 调配罐进料时段泵故障 (调配罐, 设备全部)
  - [7] 灌装机速度过低异常 (灌装机, 设备全部)
[DeviceSimulator] 启动数据生成器 (5秒异常场景)
```

### 2. 启动前端
```bash
npm run dev
```

### 3. 访问监控大屏
打开浏览器: `http://localhost:5173/dashboard`

### 4. 验证告警推送

**检查点 1: 控制台日志**
```
[AlarmStore] 新增告警: {
  id: 1,
  content: "调配罐 温度异常: 68.5 (阈值: 63.00-67.00)",
  time: "2026-04-05 14:25:30",
  severity: "medium"
}
```

**检查点 2: 右上角 ElNotification**
- 应该弹出红色错误通知
- 标题: "🚨 新告警提醒"
- 内容: "调配罐 温度异常: 68.5 (阈值: 63.00-67.00)"
- 持续时间: 5秒

**检查点 3: 自定义告警弹窗**
- 页面中央弹出告警详情框
- 3秒后自动关闭

**检查点 4: 底部告警流水**
- 告警列表实时更新
- 最新告警在最上方
- 显示时间和内容

**检查点 5: 顶部统计卡片**
- "当日异常总数" 实时增加

---

## 📊 关键配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 规则检测频率 | 5秒 | RuleEngine checkInterval |
| 告警通知持续时间 | 5秒 | ElNotification duration |
| 告警弹窗自动关闭 | 3秒 | alarmPopupVisible |
| 告警记录最大缓存 | 100条 | alarmStore MAX_RECORDS |
| 告警消息优先级 | 立即处理 | CRITICAL_TYPES 包含 ALARM_EVENT |

---

## 🎯 阶段 2 完成标志

- ✅ 告警 Store 创建完成
- ✅ 后端告警推送逻辑实现
- ✅ 前端告警接收和分发逻辑实现
- ✅ 监控大屏告警监听和弹窗实现
- ✅ 告警流水列表实时更新
- ✅ 当日异常总数实时统计

---

## 📝 下一步计划

**阶段 3: 实时统计优化**

### 待优化项:
1. **设备离线检测优化**
   - 当前: 10秒无数据 → 离线
   - 优化: 添加心跳机制,更精确判断

2. **告警去重**
   - 同一设备同一参数的重复告警,在短时间内只推送一次

3. **告警声音提示**
   - 新增告警时播放提示音

4. **连接状态指示器**
   - 在页面右上角显示 WebSocket 连接状态(已连接/断开/重连中)

5. **性能测试**
   - 测试高频告警场景(每秒10+条告警)
   - 优化批处理参数

---

## 🐛 已知问题

1. **告警弹窗遮挡问题**
   - 多条告警同时到达时,弹窗可能重叠
   - 建议: 使用队列机制,逐个显示

2. **ElNotification 堆叠**
   - 大量告警会导致通知堆叠
   - 建议: 限制同时显示的通知数量(如最多5条)

