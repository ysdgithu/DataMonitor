# 环境配置说明

## 环境变量文件优先级

Vite 会按照以下优先级加载环境变量文件（优先级从高到低）：

1. `.env.local` - **本地开发配置（最高优先级，不提交到 git）**
2. `.env.development` - 开发环境配置
3. `.env.production` - 生产环境配置
4. `.env` - 所有环境的默认配置

## 配置文件说明

### 1. `.env.local` - 本地开发环境（推荐）

**用途**：本地开发时使用，连接本地后端服务

**配置内容**：
```bash
# WebSocket 服务器地址（本地开发）
VITE_WS_URL=ws://localhost:8080

# API 服务器地址（本地开发）
VITE_API_URL=http://localhost:3002/api
```

**特点**：
- ✅ 优先级最高，会覆盖其他配置文件
- ✅ 已在 `.gitignore` 中忽略，不会提交到 git
- ✅ 适合每个开发者的本地配置

### 2. `.env.development` - 开发环境

**用途**：开发环境的默认配置

**配置内容**：
```bash
# 开发环境配置 - 本地开发
# 注意：.env.local 会覆盖此文件的配置

# WebSocket 服务器地址（本地开发）
VITE_WS_URL=ws://localhost:8080

# API 服务器地址（本地开发）
VITE_API_URL=http://localhost:3002/api
```

**特点**：
- 会提交到 git
- 如果没有 `.env.local`，则使用此配置
- 适合团队统一的开发环境配置

### 3. `.env.production` - 生产环境

**用途**：生产环境配置

**配置内容**：
```bash
# 生产环境配置 - 使用HTTPS/WSS
VITE_WS_URL=wss://cloudgu.xyz
VITE_API_URL=https://cloudgu.xyz/api
```

**特点**：
- 会提交到 git
- 执行 `npm run build` 时使用
- 适合生产环境部署

## 使用场景

### 场景 1：本地开发（连接本地后端）

1. 创建 `.env.local` 文件（如果不存在）
2. 配置本地服务器地址：
   ```bash
   VITE_WS_URL=ws://localhost:8080
   VITE_API_URL=http://localhost:3002/api
   ```
3. 启动本地后端服务：
   ```bash
   cd websocket-server
   npm run dev
   ```
4. 启动前端开发服务器：
   ```bash
   npm run dev
   ```

### 场景 2：本地开发（连接远程后端）

1. 创建 `.env.local` 文件
2. 配置远程服务器地址：
   ```bash
   VITE_WS_URL=wss://cloudgu.xyz
   VITE_API_URL=https://cloudgu.xyz/api
   ```
3. 启动前端开发服务器：
   ```bash
   npm run dev
   ```

### 场景 3：生产环境部署

1. 确保 `.env.production` 配置正确
2. 构建生产版本：
   ```bash
   npm run build
   ```
3. 预览生产版本（可选）：
   ```bash
   npm run preview
   ```

## 环境变量说明

### VITE_WS_URL
WebSocket 服务器地址，用于实时数据推送

- **本地开发**：`ws://localhost:8080`
- **生产环境**：`wss://cloudgu.xyz`（使用 WSS 加密连接）

### VITE_API_URL
API 服务器地址，用于历史数据查询和用户认证

- **本地开发**：`http://localhost:3002/api`
- **生产环境**：`https://cloudgu.xyz/api`（使用 HTTPS 加密连接）

## 调试环境配置

### 查看当前环境配置

打开浏览器控制台，在应用启动时会自动打印环境配置信息：

```
=== 环境配置信息 ===
模式: development
开发环境: true
生产环境: false
WebSocket URL: ws://localhost:8080
API URL: http://localhost:3002/api
==================
```

### 查看 API 请求配置

在 `request.ts` 中也会打印 API 配置信息：

```
[Request Config] 环境变量: {
  VITE_API_URL: "http://localhost:3002/api",
  VITE_WS_URL: "ws://localhost:8080",
  MODE: "development",
  DEV: true,
  PROD: false
}
[Request Config] API_BASE_URL: http://localhost:3002/api
```

### 查看 WebSocket 连接信息

在 `realtime.ts` 中会打印 WebSocket 配置信息：

```
[Realtime Store] 环境变量配置: {
  VITE_WS_URL: "ws://localhost:8080",
  VITE_API_URL: "http://localhost:3002/api",
  MODE: "development"
}
[Realtime Store] 使用环境变量 WebSocket URL: ws://localhost:8080
[Realtime Store] 最终 WebSocket URL: ws://localhost:8080
```

## 常见问题

### Q1: 为什么我的配置没有生效？

**A**: 检查以下几点：
1. 确认环境变量名称以 `VITE_` 开头
2. 修改环境变量后需要重启开发服务器
3. 检查 `.env.local` 是否覆盖了其他配置文件
4. 查看浏览器控制台的环境配置信息

### Q2: 接口返回 404 或连接失败？

**A**: 检查以下几点：
1. 确认后端服务已启动（`cd websocket-server && npm run dev`）
2. 确认 API URL 配置正确（包括 `/api` 后缀）
3. 检查浏览器控制台的网络请求，查看实际请求的 URL
4. 如果使用 HTTPS 页面，WebSocket 必须使用 WSS 协议

### Q3: WebSocket 连接失败？

**A**: 检查以下几点：
1. 确认 WebSocket 服务已启动（默认端口 8080）
2. 如果页面使用 HTTPS，WebSocket 必须使用 WSS 协议
3. 检查防火墙是否阻止了 WebSocket 连接
4. 查看浏览器控制台的 WebSocket 错误信息

### Q4: 如何切换开发/生产环境？

**A**: 
- **开发环境**：`npm run dev`（使用 `.env.local` 或 `.env.development`）
- **生产构建**：`npm run build`（使用 `.env.production`）
- **预览生产版本**：`npm run preview`（使用 `.env.production`）

## 最佳实践

1. ✅ **使用 `.env.local` 进行本地开发**
   - 不会提交到 git，避免配置冲突
   - 每个开发者可以有自己的配置

2. ✅ **不要在代码中硬编码 URL**
   - 始终使用环境变量 `import.meta.env.VITE_API_URL`
   - 便于在不同环境间切换

3. ✅ **生产环境使用 HTTPS/WSS**
   - 确保数据传输安全
   - 避免浏览器安全策略限制

4. ✅ **修改环境变量后重启服务**
   - Vite 只在启动时读取环境变量
   - 修改后必须重启开发服务器

5. ✅ **使用调试日志验证配置**
   - 检查浏览器控制台的环境配置信息
   - 确认实际使用的 URL 是否正确

