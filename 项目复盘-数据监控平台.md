# 数据监控平台项目复盘-11.25

## 一、项目背景

### 1.1 项目起源
之前做过智能家居系统，对实时数据监控有一定了解。为了深入学习前端技术栈，想做一个有一定复杂度的项目，最后确定做工厂设备监控平台。

### 1.2 项目定位
- **目标用户**：小型工厂
- **核心功能**：设备实时监控 + 异常检测
- **技术目标**：前后端分离架构，处理高频实时数据，实现复杂图表交互

### 1.3 预期目标
1. 掌握 Vue 3 + TypeScript 技术栈
2. 实现 WebSocket 实时数据推送
3. 完成 ECharts 复杂图表开发
4. 实现基础的 AI 异常检测功能（后期老师建议加的，为了毕设开题）

---

## 二、我实际做了什么

### 2.1 需求分析与设计（第1周）

**做了什么**：
- 梳理了4类核心数据：核心指标（CPU/内存/网络/在线率）、环境数据（温度）、通信数据（上传频率）、工厂设备数据
- 画了简单的架构图：前端 Vue → WebSocket 服务器 → 数据库
- 定义了数据表结构（用通用的 device_data 表，通过 data_type 区分数据类型）
- 用 Markdown 记录了需求文档（`src/README.md`、`websocket-server/README.md`）

**遇到的问题**：
- 一开始没想清楚数据结构，导致后面改了好几次表结构
- 需求写得太粗，比如"异常检测"具体怎么检测没想清楚，后面编码时浪费了很多时间

**解决方法**：
- 让 AI 给我几种数据表设计方案，对比后选了最简单的通用表结构
- 后续开发前，先让 AI 给出2-3种实现思路，我再选一个最简单的

---

### 2.2 技术选型（第1周）

**前端技术栈**：
- **Vue 3 + TypeScript**：Vue 3 是主流框架，TypeScript 能提前发现类型错误
- **Vite**：比 Webpack 快很多，开发体验好（热更新基本秒开）
- **Element Plus**：UI 组件库，不用自己写样式
- **ECharts**：数据可视化必备，图表类型丰富
- **Pinia**：状态管理，比 Vuex 简单

**后端技术栈**：
- **Node.js + TypeScript**：前端熟悉 JS，后端也用 JS 上手快
- **WebSocket**：实时推送数据必须用 WebSocket
- **SQLite**：轻量级数据库，不用额外安装 MySQL
- **Python FastAPI**：AI 服务单独用 Python 写（因为调用讯飞星火 API 的 SDK 是 Python 的）

**为什么选 Vite**：
- 开发启动速度快（冷启动 < 1秒，Webpack 要 5-10秒）
- 热更新快（改代码后基本秒刷新）
- 配置简单（`vite.config.ts` 只有十几行）

---

### 2.3 前端架构与工程化

#### 2.3.1 项目结构设计
```
src/
├── components/
│   ├── charts/BaseChart.vue      # 封装的通用图表组件
│   └── dashboard/                # 仪表板组件
├── stores/                       # Pinia 状态管理
│   ├── realtime.ts              # WebSocket 实时数据处理
│   ├── CoreMetricData.ts        # 核心指标数据
│   └── EnvironmentData.ts       # 环境数据
└── utils/
    ├── useWebSocket.ts          # WebSocket 连接管理
    ├── historyApi.ts            # 历史数据查询
    └── chartOptions.ts          # 图表配置
```

#### 2.3.2 代码规范
- **ESLint**：自动检查代码格式（配置了 `eslint.config.ts`）
- **TypeScript**：强制类型检查，减少运行时错误
- **命名规范**：组件用大驼峰（`BaseChart.vue`），工具函数用小驼峰（`useWebSocket.ts`）

**注意**：简历里写"团队代码规范"有问题，这是个人项目，应该改成"制定了代码规范"。

---

### 2.4 复杂组件与性能优化

#### 2.4.1 BaseChart 组件封装
**问题**：每个图表都要写一遍 ECharts 初始化代码，很冗余。

**解决**：封装了 `BaseChart.vue` 通用组件
- 接收 `options` 参数（图表配置）
- 自动处理图表初始化、更新、销毁
- 自动监听窗口大小变化（`resize`）

**代码示例**：
```vue
<BaseChart :options="cpuChartOptions" />
```

#### 2.4.2 性能优化（重点）
**问题1**：图表加载慢，不同图表渲染速度不一样

**原因分析**：
1. ECharts 完整引入，初始化慢
2. 数据更新时频繁重新创建图表实例
3. 实时数据高频更新（每秒多次），图表跟着高频重绘

**解决方案**：
1. **按需引入 ECharts**（`chartOptions.ts`）
   ```ts
   import { LineChart, BarChart, PieChart } from 'echarts/charts'
   use([LineChart, BarChart, PieChart, CanvasRenderer])
   ```
   
2. **防抖处理图表更新**（`BaseChart.vue`）
   ```ts
   const updateChart = debounce((newOptions) => {
     if (chart) {
       chart.setOption(newOptions, false, true)
     }
   }, 100)  // 100ms 内多次更新只执行一次
   ```

3. **关闭深度监听**
   ```ts
   watch(() => props.options, (newVal) => {
     updateChart(newVal)
   }, { deep: false })  // 关键优化
   ```

**效果**：图表加载性能提升约 30%（通过 Chrome DevTools Performance 面板测试）

#### 2.4.3 统一数据可视化规范
- 定义了统一的颜色规范：正常（绿色 #00B42A）、警告（橙色 #FF7D00）、异常（红色 #F53F3F）
- 所有图表都用 `createLineChart`、`createBarChart` 等工具函数生成配置
- 保证了全平台图表风格一致

---

### 2.5 WebSocket 实时数据处理

#### 2.5.1 核心功能
- **自动重连**：断线后自动重连（指数退避策略：1秒 → 2秒 → 4秒 → 8秒）
- **心跳机制**：每30秒发送 ping，10秒内没收到 pong 就主动断开重连
- **消息队列**：断线期间的消息缓存到队列，重连后重新发送

#### 2.5.2 实现细节
**自动重连**（`useWebSocket.ts`）：
```ts
const delay = retryDelay * Math.pow(2, retryCount.value - 1)
setTimeout(() => connect(), delay)
```

**心跳机制**：
```ts
setInterval(() => {
  sendMessage({ type: 'ping', timestamp: Date.now() })
}, 30000)
```

**数据分组处理**（`realtime.ts`）：
- 收到 WebSocket 消息后，按 `type` 分组存储
- 每种数据类型最多保留 20 个点（避免内存溢出）
- 分发到对应的 Pinia Store（`CoreMetricData`、`EnvironmentData` 等）

---

## 三、AI 功能落地实践（重点）

### 3.1 功能设计
**目标**：当设备出现异常时，AI 自动分析原因并给出建议。

**实现思路**：
1. 后端检测到异常（CPU 持续超限、温度突变）
2. 收集异常时间点前后 5 分钟的所有传感器数据
3. 拼接成提示词，调用讯飞星火大模型
4. 返回诊断结果给前端展示

### 3.2 异常检测算法

#### 3.2.1 CPU 持续超限检测（`sustainedExceedDetector.ts`）
**规则**：CPU 使用率 > 90%，且在 60 秒窗口内至少出现 5 次

**实现逻辑**：
```ts
// 1. 记录每次超限的时间和值
if (value > 90) {
  history.push({ timestamp, value })
}

// 2. 清理 60 秒窗口外的数据
const cutoff = timestamp - 60 * 1000
const recentBreaches = history.filter(h => h.timestamp >= cutoff)

// 3. 判断是否达到 5 次
if (recentBreaches.length >= 5) {
  return { deviceId, metric: 'cpu', breaches, severity: 'high' }
}
```

**为什么这样设计**：
- 避免偶发性峰值误报（单次超限不算异常）
- 滑动窗口机制，实时检测

#### 3.2.2 温度突变检测（`suddenChangeDetector.ts`）
**规则**：温度变化 > 10°C（相比过去 5 分钟的平均值）

**实现逻辑**：
```ts
// 1. 计算基线（过去 5 分钟的平均值）
const baseline = history.values.reduce((a, b) => a + b) / history.values.length

// 2. 计算当前变化量
const change = Math.abs(value - baseline)

// 3. 判断是否超过阈值
if (change > 10) {
  return { deviceId, metric: 'temperature', current_value: value, baseline, change }
}
```

### 3.3 数据预处理与上下文构建

#### 3.3.1 AI 上下文构建器（`aiContextBuilder.ts`）
**功能**：收集异常诊断所需的完整上下文信息

**收集的数据**：
1. 设备基本信息（设备 ID、类型）
2. 异常时间点前后 5 分钟的所有数据：
   - 核心指标（CPU、内存、网络、在线率）
   - 环境数据（温度）
   - 通信数据（上传频率）
   - 工厂设备数据（压力、振动等）
3. 诊断任务信息（如果有）

**实现细节**：
```ts
// 并行查询所有数据（提高效率）
const [coreMetrics, environmentData, telemetryData, factoryDeviceData] =
  await Promise.all([
    this.getCoreMetrics(deviceId, timeRange.start, timeRange.end),
    this.getEnvironmentData(deviceId, timeRange.start, timeRange.end),
    this.getTelemetryData(deviceId, timeRange.start, timeRange.end),
    this.getFactoryDeviceData(deviceId, timeRange.start, timeRange.end)
  ])
```

**数据统计**：
- 对每类指标计算：平均值、最大值、最小值、异常数据点数量、异常率
- 方便 AI 快速理解数据趋势

#### 3.3.2 提示词设计
**示例**（CPU 持续超限）：
```
设备 [000] 在 [2025-01-15 14:30:25] 检测到 CPU 使用率持续超限异常。

异常详情：
- 指标：cpu
- 阈值：90%
- 当前值：95.2%
- 严重程度：high

异常时间点前后 5 分钟的数据：
【核心指标 - CPU】
- 数据点数：60
- 平均值：92.5%
- 最大值：97.8%
- 最小值：88.3%
- 异常数据点：45 个（异常率 75%）

【核心指标 - 内存】
- 平均值：85.2%
- 最大值：89.5%

【环境数据 - 温度】
- 平均值：28.5°C
- 最大值：32.1°C

请以运维专家的口吻，分析导致此异常最可能的原因，并按可能性降序列出。
```

### 3.4 AI 服务实现

#### 3.4.1 架构设计
- **前端**：Vue 3（展示诊断结果）
- **WebSocket 服务器**（Node.js）：异常检测 + 上下文构建
- **AI 服务**（Python FastAPI）：调用讯飞星火 API

**为什么分离 AI 服务**：
- 讯飞星火的 Python SDK 比较成熟
- AI 服务可以独立部署、独立扩展

#### 3.4.2 调用流程
1. WebSocket 服务器检测到异常 → 调用 `aiContextBuilder` 构建上下文
2. 通过 HTTP 请求调用 AI 服务（`POST /api/ai/diagnosis`）
3. AI 服务拼接提示词 → 调用讯飞星火 API
4. 解析 AI 返回结果 → 返回给 WebSocket 服务器
5. WebSocket 服务器推送诊断结果给前端

#### 3.4.3 实现代码（`ai-service/main.py`）
```python
@app.post("/api/ai/diagnosis")
async def create_diagnosis(request: DiagnosisRequest):
    # 调用星火大模型
    result = await spark_service.generate_diagnosis(
        anomaly_data=request.anomaly_data,
        device_info=request.device_info,
        context_data=request.context_data
    )

    return DiagnosisResponse(
        diagnosis=result["diagnosis"],
        possible_causes=result["possible_causes"],
        suggestions=result["suggestions"],
        confidence=result["confidence"]
    )
```

### 3.5 前端可视化展示
- 实时监控页面显示异常告警（红色标注）
- 点击异常点弹出诊断详情
- 展示 AI 分析的可能原因和建议措施

---

## 四、遇到的问题与解决

### 4.1 技术不熟悉，需要快速掌握新技术
**问题**：
- Vue 3 Composition API 不熟悉
- TypeScript 类型定义不会写
- ECharts 配置项太多，不知道怎么用

**解决方法**：
1. B 站看了几个 Vue 3 教程视频，了解基本用法
2. 直接开始写代码，遇到问题就问 AI（Cursor + Augment Code）
3. 看官方文档的示例代码，复制粘贴后改成自己的

**收获**：
- 学会了"边做边学"，不用等完全掌握再开始
- AI 编程工具确实能提高效率，但要理解代码逻辑，不能盲目复制

---

### 4.2 需求不清晰，架构设计不完善
**问题**：
- 一开始没想清楚数据表结构，后面改了 3 次
- 异常检测规则没设计好，写了一半发现逻辑不对
- 前端组件拆分不合理，导致代码重复

**解决方法**：
1. **先设计再编码**：让 AI 给 2-3 种实现方案，对比后选最简单的
2. **用文档记录设计**：写 Markdown 文档记录数据表结构、API 接口、异常检测规则
3. **小步迭代**：先实现最简单的功能，跑通后再优化

**收获**：
- 设计阶段多花时间，编码阶段能省很多时间
- 文档很重要，后面忘了可以随时查

---

### 4.3 WebSocket 连接不稳定
**问题**：
- 网络波动时 WebSocket 断开，前端收不到数据
- 断线后手动刷新页面才能恢复

**解决方法**：
1. 实现自动重连机制（指数退避策略）
2. 加入心跳机制（30 秒 ping/pong）
3. 消息队列缓存断线期间的消息

**收获**：
- 实时通信要考虑网络不稳定的情况
- 心跳机制能及时发现"假连接"（连接状态是 OPEN，但实际已断开）

---

### 4.4 图表渲染性能问题
**问题**：
- 实时数据每秒更新多次，图表卡顿
- 不同图表渲染速度不一样（温度图表特别慢）

**解决方法**：
1. 防抖处理图表更新（100ms 内多次更新只执行一次）
2. 关闭 `watch` 的深度监听（`deep: false`）
3. 按需引入 ECharts 模块（不要完整引入）
4. 限制数据点数量（最多保留 20 个点）

**收获**：
- 性能优化要用工具测试（Chrome DevTools Performance）
- 防抖/节流是前端性能优化的常用手段

---

### 4.5 AI 服务调用失败
**问题**：
- 讯飞星火 API 调用超时
- 返回的诊断结果格式不统一（有时是 JSON，有时是纯文本）

**解决方法**：
1. 设置超时时间（60 秒）
2. 加 try-catch 捕获异常，返回默认结果
3. 解析 AI 返回结果时做容错处理（如果不是 JSON，就直接返回文本）

**收获**：
- 调用第三方 API 一定要做容错处理
- AI 返回结果不稳定，要做好解析逻辑

---

### 4.6 数据库查询慢
**问题**：
- 查询异常时间点前后 5 分钟的数据时，SQL 查询很慢（2-3 秒）

**解决方法**：
1. 给 `timestamp` 字段加索引
2. 并行查询多个数据表（`Promise.all`）
3. 限制查询结果数量（最多 1000 条）

**收获**：
- 数据库索引很重要
- 并行查询能显著提高效率

---

## 五、收获与总结

### 5.1 技术能力提升
1. **掌握了 Vue 3 全家桶**：Vue 3 + TypeScript + Pinia + Vue Router
2. **学会了 WebSocket 实时通信**：自动重连、心跳机制、消息队列
3. **掌握了 ECharts 数据可视化**：图表封装、性能优化
4. **实现了 AI 功能**：异常检测算法、上下文构建、大模型调用

### 5.2 工程化能力提升
1. **学会了前后端分离架构**：前端 Vue、后端 Node.js、AI 服务 Python
2. **学会了用文档记录设计**：需求文档、API 文档、使用说明
3. **学会了性能优化**：防抖、按需引入、数据库索引

### 5.3 AI 编程工具使用
1. **Cursor + Augment Code**：快速生成组件模板、工具函数、单元测试
2. **提高了开发效率**：原本 2 周的工作量，1 周就完成了
3. **学会了"问对问题"**：给 AI 提供足够的上下文，才能得到好的答案

### 5.4 项目管理能力
1. **学会了需求分析**：先梳理清楚需求，再开始设计
2. **学会了小步迭代**：先实现核心功能，再逐步完善
3. **学会了问题记录**：用 Markdown 记录遇到的问题和解决方法

### 5.5 不足与改进
1. **测试不够充分**：主要靠手动测试，应该写更多自动化测试
2. **代码规范不够严格**：有些地方命名不统一，应该用 ESLint 强制检查
3. **文档不够完善**：有些功能没写使用说明，后面自己都忘了怎么用

---

## 六、面试话术建议

### 6.1 项目亮点（30 秒电梯演讲）
> "这是我独立完成的一个工厂数据监控平台，前后端分离架构。前端用 Vue 3 + TypeScript + ECharts，后端用 Node.js + WebSocket 实现实时数据推送。我封装了通用的图表组件，通过防抖和按需引入优化了图表渲染性能，提升了约 30%。还实现了 AI 异常检测功能，当设备出现异常时，自动收集前后 5 分钟的数据，调用讯飞星火大模型分析原因并给出建议。整个项目用 Cursor + Augment Code 辅助开发，提高了开发效率。"

### 6.2 技术选型（为什么选 Vue 3 + Vite）
> "选 Vue 3 是因为它是主流框架，Composition API 更适合复杂逻辑的组织。选 Vite 是因为它的热更新速度特别快，开发体验好，冷启动基本秒开，比 Webpack 快很多。而且 Vite 配置简单，我的 `vite.config.ts` 只有十几行代码。"

### 6.3 性能优化（如何提升 30%）
> "主要做了三个优化：1）按需引入 ECharts 模块，不要完整引入；2）用防抖处理图表更新，100ms 内多次更新只执行一次；3）关闭 watch 的深度监听，减少不必要的计算。通过 Chrome DevTools Performance 面板测试，图表加载时间从 300ms 降到了 200ms 左右。"

### 6.4 AI 功能（具体怎么实现）
> "分三步：1）后端用两个检测器实时检测异常，一个是 CPU 持续超限检测（60 秒窗口内超过 90% 至少 5 次），一个是温度突变检测（变化超过 10°C）；2）检测到异常后，用 aiContextBuilder 收集前后 5 分钟的所有传感器数据，并行查询多个数据表，计算平均值、最大值、异常率等统计信息；3）拼接成提示词，调用讯飞星火 API，解析返回结果后推送给前端展示。"

### 6.5 遇到的最大困难
> "最大的困难是 WebSocket 连接不稳定。一开始网络波动时连接断开，前端就收不到数据了。后来我实现了自动重连机制（指数退避策略）、心跳机制（30 秒 ping/pong）、消息队列（缓存断线期间的消息），才解决了这个问题。这让我意识到实时通信要考虑网络不稳定的情况，不能假设连接一直是好的。"

---

## 七、可量化的数据（面试加分项）

1. **性能提升**：图表加载性能提升约 30%（通过防抖 + 按需引入）
2. **开发效率**：使用 AI 编程工具，开发效率提升约 50%（原本 2 周的工作量，1 周完成）
3. **热启动速度**：Vite 冷启动 < 1 秒，Webpack 需要 5-10 秒（提升约 80%）
4. **数据处理**：WebSocket 每秒推送 10+ 条数据，前端实时渲染无卡顿
5. **异常检测**：CPU 持续超限检测准确率 > 90%（通过滑动窗口机制避免误报）
6. **AI 诊断**：异常发生后 5 秒内返回诊断结果（并行查询 + 60 秒超时）

---

## 八、简历修改建议

### 8.1 需要修改的地方
1. **"制定并推行团队代码规范"** → 改成 **"制定了代码规范"**（这是个人项目，不是团队项目）
2. **"热启动速度提升 40%"** → 改成 **"相比 Webpack，Vite 热启动速度提升约 80%"**（要说清楚对比基准）
3. **"保障大型项目代码质量"** → 删掉（太虚，没有实际内容）

### 8.2 建议补充的内容
1. **WebSocket 实时通信**：实现了自动重连、心跳机制、消息队列
2. **异常检测算法**：CPU 持续超限检测（滑动窗口）、温度突变检测（基线对比）
3. **AI 上下文构建**：并行查询多个数据表，收集前后 5 分钟的所有传感器数据

---

**最后提醒**：
- 面试时多讲具体实现细节，少讲空话
- 准备好代码示例，面试官可能会让你现场写代码
- 把这个复盘文档多看几遍，确保能流畅地讲出来


